# US-12.3 : Indicateur visuel "en revue" sur le board Kanban ✅ COMPLÉTÉ

## En tant que utilisateur du board Kanban Sprint

## Je souhaite voir un indicateur visuel sur la carte US lorsqu'elle est en phase de revue par le Lead Dev

## Afin de comprendre immédiatement que l'US est entre deux étapes et attend validation avant de passer à l'agent suivant

# Critères d'acceptation

**CA1 - Marquage de l'état "en revue" dans US en cours.md** :
- Le fichier `US en cours.md` accepte un suffixe `-review` sur l'étape (ex: `BDD-review`, `TDD-back-end-review`, `TDD-front-end-review`)
- Le suffixe `-review` est reconnu comme un état intermédiaire entre deux étapes
- Les étapes valides avec suffixe sont : `US-review`, `BDD-review`, `TDD-back-end-review`, `TDD-front-end-review`

**CA2 - Lecture de l'état "en revue" par sprintBoardReader** :
- La fonction de lecture du board détecte le suffixe `-review` dans l'étape
- L'information `enRevue: true/false` est exposée dans les données de l'US
- L'étape de base (sans suffixe) reste accessible pour le positionnement dans la colonne

**CA3 - Affichage de l'indicateur visuel sur la carte** :
- Un badge ou icône 🔍 apparaît sur la carte US quand `enRevue` est `true`
- L'indicateur est visible sans surcharger la carte (discret mais lisible)
- L'indicateur disparaît automatiquement quand l'étape passe au suivant ou à `done`

**CA4 - Cohérence avec le workflow existant** :
- L'indicateur n'affecte pas le positionnement de la carte dans la colonne (elle reste dans la colonne de l'étape de base)
- Le comportement du clic sur la carte (modal détaillée) reste inchangé
- Les US sans suffixe `-review` s'affichent normalement sans indicateur

### Fichiers concernés

- `data/A propos/Sprints/US en cours.md` — ajout du suffixe `-review`
- `utils/sprintBoardReader.ts` — détection du suffixe et exposition de `enRevue`
- `app/(main)/a-propos/` — affichage du badge/icône